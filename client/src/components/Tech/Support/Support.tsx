import { FC } from 'react'
import './Support.scss'
import { observer } from 'mobx-react-lite'

const Support:FC = () => { 

  return(
    <div>
      support
    </div>
  )
}

export default observer(Support)