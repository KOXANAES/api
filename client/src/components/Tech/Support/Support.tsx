import { FC } from 'react'
import './Support.css'
import { observer } from 'mobx-react-lite'

const Support:FC = () => { 

  return(
    <div>
      support
    </div>
  )
}

export default observer(Support)